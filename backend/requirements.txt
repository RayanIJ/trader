# Backend (FastAPI) dependencies for the automated options trading cockpit.
# The TWS socket API client (ibapi) is vendored under IB.1037.02/ and imported lazily.
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
SQLAlchemy>=2.0.29
PyYAML>=6.0.1
cryptography>=42.0.0
python-dotenv>=1.0.1
pytz>=2024.1

# Required by the bundled TWS API client (ibapi 10.37 uses google.protobuf).
protobuf>=5.26.0

# Dev / test
pytest>=8.1.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
